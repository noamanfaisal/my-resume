node_modules/
dist/
.DS_Store
*.log
.env
.env.local

# Generated automatically by scripts/parse-data.js from resume-data/*.md
# every time `npm run dev` or `npm run build` runs (via predev/prebuild
# hooks in package.json). Never edit these directly — they get overwritten.
# Not committed since they're always rebuilt fresh, including on Vercel.
src/data/data.json
public/cv.md
